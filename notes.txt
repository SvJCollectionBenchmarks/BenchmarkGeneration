Add isEmpty method to all
Separate immutable collections to another group maybe (don't compare it with mutables explicitly)?
args of methods should be divided into languages

ARGUMENTS SHOULD BE GENERATED ONCE PER GROUP, NOT PER METHOD:
    1. Group your operations by profiles they are going to be set in;
    2. For one of the profiles (traditionally Java) generate arguments based on the type to instance function;
    3. For all other profiles, check if a newly made profile transition function can reapply similar values;
    4. If the transition is not supported, generate a new argument based on the rules from the 2. point;

Good mod for the primes benchmark: populate the map with all the numbers in the beginning, delete keys that are not prime.

Planowane części pracy:
	- Pomiar operacji w separacji - nie potrzebuje generacji
		- Problemy z clear
	- Pomiar operacji w wymyślonych scenariuszach - nie wymaga generacji, ale w przypadku zmian w grupach może się ona okazać bardzo dobra
	- Pomiar operacji w pseudo-losowych złożeniach - generacja jest bardzo pomocna
		- Problemy z clear

Wymagania minimalne:
	- Przyjrzeć się zestawom kolekcji i ich operacjom
	- Spisać scenariusze dla wyznaczonych zestawów
	- Pomierzyć wyznaczone grupy kolekcji
	- Policzyć, czy wyniki istotnie się różnią między sobą (przedziały ufności)
	- Opracować wyniki w formie graficznej

Wymagania docelowe:
	- Uwzględnić wielowątkowe zastosowania (yikes) [For memory usage you can add GC or HS_GC profiler using the addProfiler method when building runner options.]
	- Uwzględnić zastosowania w przeszukiwaniu (można pokryć odpowiednimi grupami)
    - Uwzględnić wymagania pamięciowe kolekcji (yikes)