Add isEmpty method to all
Add prefixes to collections to avoid import mishaps
Why did you have to generate manually written benchmarks?
Add to groups in a new section the same type variables as were added to groups, so that we can generate arguments;