#{groups.$group.operations.addAll.$profile.args.type, T = Integer} elems = ${benchmarks.$benchmark.variables.profiled.$profile.elemsOne};
#{groups.$group.init.$profile.content, T = Integer};
#{groups.$group.operations.add.$profile.content, elem = 2};
#{groups.$group.operations.addAll.$profile.content, elems = elems};
#{groups.$group.operations.remove.$profile.content, elem = 3};
bh.consume(#{groups.$group.operations.get.$profile.content, index = 3});
#{groups.$group.operations.clear.$profile.content};